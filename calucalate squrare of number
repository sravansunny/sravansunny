#include <stdio.h>
int main() {
    float num,square;
    printf("Enter a number to calculate square: ");
    scanf("%f", &num);
    square = num * num;
    printf("Square of %.2f = %.2f",num, square);
    return 0;
}
